Apache ant based YUI compresror tool.

Run the ant from the compressor root. Change build file as neccessory.

Usage: Can be use this to combine and compress both CSS and JS files as separate two files
